WHAT: none
WHERE: none
WHEN: 0 - 24 
WHO: none
OTHER: none

    SceneStart()
    If Actor.isDating()
        "La meilleure chose à propos d'une relation stable est que le sexe est beaucoup plus facile à trouver"
    Else
        "J'ai déjà couché avec <Actor.name>. Si <Actor.il_or_elle> n'a pas changé, je n'ai pas besoin de beaucoup d'arguments pour <Actor.le_or_la> convaincre de faire un trip de cul?"
    Endif
    "Dois-je être direct et suggérer de faire un trio avec <Actor.name> et une autre personne que je connais maintenant?"
    0:: "Oui"
    1:: "Non"
    
    If 0
        If Random(0, 50) < Actor:attractiontoplayer + interpersonal + Actor:perversion + Actor:incest
            Actor(Flirty):: "Très bien ... Je suis moi-même assez en manque en fait. A qui pense-tu pour nous rejoindre?"
            selectNPC()
            Actor2 = getSelectedNPC()
            If !Actor2.isValid()
                "Je ne suis pas capable de me décider, qui inviter."
            Elseif Actor2.hadSex() || Actor2.isInterestedIn(Actor)
                Actor2.dress()
                Actor2.show()
                "Nous avons réussi à convaincre <Actor2.name> de rejoindre notre trio. En un rien de temps, <Actor2.he_or_she> est arrivé."
                Sex(Player, Actor, Actor2)
                perversion += Random(0, 0.05)            
            Else
                "Malheureusement, <Actor2.name> a refusé notre invitation pour un trio. Je devrais peut-être choisir quelqu'un avec qui j'ai déjà couché la prochaine fois, ou quelqu'un qui s'intéresse à <Actor.name>. Un trio n'est généralement pas une demande fait du bleu." 
            Endif
        Else
            Actor(Sad):: "Désolé, mais je ne suis pas dans le beat pour faire du cul en ce moment, encore moins un trio ..."
        Endif
    Endif
    SceneEnd()