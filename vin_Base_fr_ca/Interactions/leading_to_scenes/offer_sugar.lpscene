WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    "Qui va faire le <Player.sugardaddy_or_sugarmommy> entre nous deux?"
    0:: "<Actor.name> va être le <Actor.sugardaddy_or_sugarmommy>"
    1:: "Je vais être le <Player.sugardaddy_or_sugarmommy>"
    
    If 0
        SugarParent = SugarParent.getGlobal()
        If SugarParent > 0
            "Je dois garder à l'esprit que j'ai déjà un arrangement avec quelqu'un. Si <Actor.name> devient <Actor.mon_or_ma> <Actor.sugardaddy_or_sugarmommy>, il est presque certain qu'<Actor.il_or_elle> voudra que notre arrangement soit exclusif et je ne peux pas briser l'autre arrangement maintenant. Je ne peux pas avoir le pain et le beurre en même temps!"
        Endif
        If Actor.isInterestedIn(Player) && Actor:intelligence + Actor:interpersonal + Actor:perversion + attractiveness - Actor:attractiveness + Actor:age - age > 150
            Actor(Flirty):: "Eh bien, je ne suis pas contre de devenir <Actor.ton_or_ta> <Actor.sugardaddy_or_sugarmommy> et j'aime à quel point tu es direct à me le demander."
            Actor(Flirty):: "Que diriez-vous d'une première rencontre question de régler notre arrangement?"
            0:: "Oui"
            1:: "Non"
            
            If 0
                "Pourquoi pas? Je suis sûr que <Actor.name> sera très génér<Actor.eux_or_euse> à notre première rencontre."
                Loc = findNearbyBuilding(restaurant)
                moveTo(Loc)
                "<Actor.name> m'a emmené dans un restaurant très chic et m'a payé un verre et un dîner comme si l'argent n'était pas un problème. Ensuite, <Actor.il_or_elle> a suggéré que nous allions dans une suite qu'<Actor.il_or_elle> avait réservé dans un hôtel 5 étoiles à proximité."
                0:: "Oui"
                1:: "Non"
                
                If 0
                    Loc = findNearbyBuilding(hotel)
                    moveTo(Loc)
                    setBackground(home)
                    Actor.makePermanent()
                    "Wow, c'est une belle suite d'hôtel! Cela a dû coûter une fortune. Et je sais très bien ce que <Actor.name> attend de moi pour sa générosité."
                    Sex(Actor, Player)
                    perversion += Random(0, 0.5)
                    Actor.show(2)
                    "Le sexe n'était pas trop mal. Nous avons passé la nuit ensemble. Le lendemain matin, <Actor.name> a payé le petit-déjeuner, et avant de partir travailler, <Actor.il_or_elle> m'a dit qu'<Actor.il_or_elle> m'appellerait bientôt et me laisserait une enveloppe avec un peu d'argent. Inutile de vous dire qu'<Actor.il_or_elle> est maintenant <Actor.mon_or_ma> <Actor.sugardaddy_or_sugarmommy>."
                    money += Random(100, 500)
                    SugarParent = Actor.getID()
                    SugarParent.setGlobal(SugarParent)
                Else
                    "J'ai refusé l'offre de <Actor.name> d'aller à l'hôtel. <Actor.name> était visiblement déçu et il va sans dire qu'<Actor.il_or_elle> ne m'a plus jamais contacté."
                Endif
            Else
                "Ew, après réflexion, même pour de l'argent, je ne peux pas coucher avec quelqu'un d'aussi vieux et lai<Actor.d_or_de>."
            Endif            
        Else
            Actor(Sarcastic):: "Désolé, mais vous avez dû me prendre pour un millionnaire avec de l'argent à brûler. Je dois dire que je suis un peu flatté. Mais non!" 
        Endif
    Else
        SugarBabe = SugarBabe.getGlobal()
        If SugarBabe > 0
            "Je dois garder à l'esprit que j'ai déjà un arrangement avec quelqu'un côté sugar dating. Si <Actor.name> devient <Actor.mon_or_ma> <Actor.sugarboy_or_sugargirl>, je devrai probablement me débarrasser de l'autre arrangement. Deux en même temps, c'est tout simplement hors de mon budget."
        Endif
        If Actor.isInterestedIn(Player) && Actor:perversion - Actor:intelligence + Actor:attractiontoplayer > 50
            Actor(Flirty):: "Eh bien, je suis fauché alors ... Ok pourquoi pas! J'aime à quel point tu es direct avec moi."
            Player(Flirty):: "Que diriez-vous d'un premier rendez-vous pour régler notre arrangement?"
            "Je vais m'arranger pour être très génér<Player.eux_or_euse> lors du premier rendez-vous."
            Loc = findNearbyBuilding(restaurant)
            moveTo(Loc)
            "J'ai emmené <Actor.name> dans un restaurant vraiment chic et j'ai tout payé pour <Actor.name> comme si l'argent poussait dans les arbres. Ensuite, je lui ai suggéré que nous nous allions dans une suite que j'avais réservée dans un hôtel 5 étoiles à proximité."

            Loc = findNearbyBuilding(hotel)
            moveTo(Loc)         
            setBackground(home)
            Actor.makePermanent()
            "Cette suite d'hôtel m'a coûté une fortune. Et j'espère que <Actor.name> sait ce que j'attends de <Actor.lui_or_elle> en échange de ma générosité."
            Sex(Player, Actor)
            perversion += Random(0, 0.5)
            Actor.show(2)
            "Nous avons passé la nuit ensemble. Le lendemain matin, j'ai payé le petit-déjeuner et avant de partir travailler, j'ai dit à <Actor.name> que je l'appellerai bientôt et que je laisserai une enveloppe généreuse sur la table. Inutile de dire que <Actor.name> est maintenant <Actor.mon_or_ma> <Actor.sugarboy_or_sugargirl>."
            money -= Random(100, 500)
            SugarBabe = Actor.getID()
            SugarBabe.setGlobal(SugarBabe)
        Else
            Actor(Angry):: "Je ne suis pas si pauvre que cela et l'amour ne s'achète pas. À peu près sûr, il existe des sites Web sur lesquels vous pouvez vous rendre pour ce genre de choses - vous n'avez pas besoin d'approcher des jeunes au hasard pour les fourrer en échange de cadeaux."
            Player(Happy)::"Bon, bon, regarde la chochotte, c'était seulement une question, pas obligé dans faire un cas!"
            Actor:rapportwithplayer -= Random(0, 5)
        Endif
    Endif
    SceneEnd()