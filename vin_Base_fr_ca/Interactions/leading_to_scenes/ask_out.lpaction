ACTION_UNIQUEID: ask_out
ACTION_NAME: Demandez à sortir ensemble
WHERE: interaction
WHEN: 0 - 24
MINUTES: 5 - 10
TIMEOUT_MINUTES: 0
ALSO_TIMEOUT: 
EFFECTS:
CONDITIONS:
MOVE_FIRST: true
SCENE_ALWAYS: ask_out
ANIMATION:
WHO: Actor = getTarget(); If !Actor.isTemporary() && !isWithCompanion() && !isWithCompanionCreature() && !Actor.isCreature()