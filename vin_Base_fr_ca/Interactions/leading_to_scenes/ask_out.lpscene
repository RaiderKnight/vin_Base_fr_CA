WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    If Actor.isDating() || Actor.isRelative() || [Random(0, 100) < interpersonal && [Actor:rapportwithplayer > 10 || Actor:attractiontoplayer > 10]] 
        Actor(Happy):: "Bien sûr, je te suis!"
        Actor.startDate()
    Else
        Actor(Sad):: "Désolé, je n'ai pas envie de sortir avec toi aujourd'hui."
    Endif
    SceneEnd()