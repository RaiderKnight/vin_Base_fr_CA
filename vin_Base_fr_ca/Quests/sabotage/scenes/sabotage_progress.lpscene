WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
    Actor = a1.getActorAlias()
    Actor2 = a2.getActorAlias()
    
    relation = sabotage_relation.getGlobal()
    detection = sabotage_detection.getGlobal()
    
    If relation <= 0
        "Hmm, une nouvelle mise à jour Facebook ..."
        Actor.dress()
        Actor.show(2)
        Actor2.dress()
        Actor2.show(3)
        "<Actor.name> <Actor.name_last> est désormais célibataire."
        "<Actor2.name> <Actor2.name_last> est désormais célibataire."
        removeNPCRelationship(Actor, Actor2)
        "Haha! Mon effort de sabotage a parfaitement fonctionné. Leur relation est officiellement terminé."
        karma -= 10
        intelligence += 5
        QuestEnd()
    Elseif detection >= 100
        Actor.dress()
        Actor.show(2)
        Actor2.dress()
        Actor2.show(3)
        Actor(Angry):: "Hey <Player.le_or_la> comique, pense-tu vraiment que nous n'avons pas remarqué ce que tu essaye de faire?"
        Actor2(Angry):: "Pourquoi essaye-tu de ruiner notre relation? Est-tu jaloux ou tu as juste vraiment pas de vie?"
        Actor(Angry):: "Et devine quoi: tu as même renforcé notre relation, elle est même plus forte. Ont veux plus de revoir la face, ti-coune!"
        "Oh non, <Actor.name> et <Actor2.name> ont compris mon plan. Mon effort de sabotage a complètement échoué."
        blockContact(Actor)
        blockContact(Actor2)
        Actor:rapportwithplayer => -100
        Actor2:rapportwithplayer => -100
        Actor:attractiontoplayer => -100
        Actor2:attractiontoplayer => -100       
        QuestEnd()
    Else   
        "La force de la relation actuelle entre <Actor.name> et <Actor2.name> est: <relation>."
        "Le niveau de suspicion qu'ils ont à propos de ma tentative de sabotage est: <détection>."
    Endif
    SceneEnd()