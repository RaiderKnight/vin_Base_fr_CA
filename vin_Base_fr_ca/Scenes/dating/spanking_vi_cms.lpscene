WHAT: all
WHERE: all
WHEN: 0-24
WHO: Actor = getCompanion(); If Actor:martial - Actor:masochist + masochist - martial > Random(50, 200)
OTHER: none

    SceneStart()
    Actor(Evil):: "Tu as été méchan<Player.t_or_te>. Prépare-toi à recevoir une fessée!"
    "Ce n'est un secret pour personne que <Actor.name> est un peu dominan<Player.t_or_te>."
    0:: "Recevoir la fessée"
    1:: "Résister"
    
    If 0
        "Je dois admettre que je suis devenu un peu maso et que j'ai commencé à en profiter."
        "<Actor.name> m'a donné plusieurs fessée sur les fesses avant de me laisser partir."
        masochist += 1
    Else
        Player(Angry):: "Cela va trop loin!"
        masochist -= 1
    Endif
    SceneEnd()
    timeout(200, spanking_vi_cms)