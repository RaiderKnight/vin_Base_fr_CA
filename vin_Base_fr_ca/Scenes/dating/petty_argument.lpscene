WHAT: all, -sleep, -nap
WHERE: home
WHEN: 0 - 24
WHO: Dating = getSpecific(Dating); If Dating:attractiontoplayer < Random(0, 70)*Random(0, 1)*Random(0, 1)
OTHER: !isWithCompanion()

    SceneStart()
    Dating.dress()
    Dating.show(2)

    "<Dating.name> et moi avons une dispute assez stupide sur des choses triviales. Comment dois-je agir?"
    0:: "Reculer"
    1:: "Essayer de gagner mon point"

    If 0
        "J'ai décidé de laisser tomber et je lui ai donné raison pour que ça ce termine."
        masochist += Random(0, 0.5)
        karma += 2.5
    Else
        "Il en n'est pas question que je recule. Je me suis assuré que <Dating.name> sache que c'est moi qui a raison, un point c'est tout, that's it !"
        Dating:rapportwithplayer -= Random(0, 1)
    Endif

    timeout(250, petty_argument)
    SceneEnd()