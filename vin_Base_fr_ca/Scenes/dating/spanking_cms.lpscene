WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getSpecific(Dating); If Actor:martial - Actor:masochist + masochist - martial > Random(50, 200)
OTHER: none

    SceneStart()
    Actor(Evil):: "Vous avez été méchant. Prépare-toi a recevoir une fessée!"
    "Ce n'est un secret pour personne que <Actor.name> est un peu un <Actor.dominateur_or_dominatrice>."
    0:: "Accepter la fessée"
    1:: "Résister"
    
    If 0
        "Je dois admettre que je suis devenu un peu un esclave sexuel mais je commence à en profiter."
        "<Actor.name> me donne plusieurs fessées sur les fesses avant de me laisser partir."
        masochist += 1
    Else
        Player(Angry):: "Arrête, ça va trop loin!"
        masochist -= 1
    Endif
    SceneEnd()
    timeout(200, spanking_cms)