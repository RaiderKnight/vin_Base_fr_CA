WHAT: all, -sleep_hotel, -nap_hotel, -go_to_the_bathroom
WHERE: all, -home, -work
WHEN: 0 - 24
WHO: Actor = getSpecific(ExDating); If !Actor.isAsexual() && Actor:masochist < 0
OTHER: none

    SceneStart()
    "Je marchais tranquillement quand tout à coup, je repère une silhouette familière main dans la main avec quelqu'un d'autre."
    Actor.dress()
    Actor.show(2)
    NewDating = Actor.getRelatedPerson(Dating, Spouses)
    If !NewDating.isValid()
        NewDating = generatePersonTemporary()
        While !Actor.isInterestedIn(NewDating)
            NewDating = generatePersonTemporary()
        Endwhile
        NewDating.makePermanent()
        addNpcRelationship(Dating, Actor, NewDating)
    Endif
    NewDating.dress()
    NewDating.show(3)
    "C'est mon ex <Actor.name>! <Actor.Il_or_Elle> m'a tout de suite reconn<Actor.u_or_ue>. Question de tourner le fer dans la plaie, <Actor.il_or_elle> s'est soudainement penché pour embrasser <NewDating.son_or_sa> <NewDating.chum_or_blonde> devant moi. Ça doit être assez réçent pour qu'ils s'embrassent autant!>."
    0:: masochist < 50:: "Ignorer"
    1:: "Crier son noms"
    
    If 0
        "Je m'en fou, c'est son choix? Anyway, je <NewDating.le_or_la> comprend pas, <NewDating.son_or_sa> <NewDating.chum_or_blonde> est <NewDating.laid_or_laide> comme l'enfer! <Actor.name> a dû baisser ces standards de sélection après moi. Pff...!"
        "Je les ai ignorés et j'ai continué de marcher, comme si je n'avais jamais rien vu du tout."
        masochist -= Random(0, 2)
    Else
        Player(Crying):: "..."
        "Je ne pouvais tout simplement pas retenir mes larmes. Voir mon ex avec une autre personne était trop blessant."
        masochist += Random(0, 2)
    Endif
    
    SceneEnd()
    timeout(1500, come_across_ex_dating)