WHAT: all
WHERE: all
WHEN: 0-24
WHO: Actor = getCompanion(); If martial - masochist + Actor:masochist - Actor:martial > Random(50, 200)
OTHER: none

    SceneStart()
    "Je suis beaucoup plus dominant que <Actor.name>. Peut-être que je devrais lui donner une fessée pour montrer qui est le boss ici!"
    0:: "Lui donner une fessée"
    1:: "C'est trop vège"
    
    If 0
        Player(Evil):: "Tu as été méchan<Actor.t_or_te>. Prépare-toi à une fessée!"       
        If martial - masochist + Actor:masochist - Actor:martial > Random(50, 200)
            Actor:: "Maitre, je dois admettre que je suis devenu un peu maso et que j'aime recevoir la fessée par tes mains."
            "Je lui ai donné une fessée plusieurs fois sur les fesses avant de <Actor.le_or_la> laisser partir."
            Actor:masochist += 1
        Else
            Actor(Angry):: "Cela va trop loin!"
            Actor:masochist -= 1
            Actor:attractiontoplayer -= 1
        Endif
    Endif
    SceneEnd()
    timeout(200, spanking_companion_cms)