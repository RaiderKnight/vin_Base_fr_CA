WHAT: all
WHERE: home
WHEN: 0 - 24
WHO: Actor = getSpecific(Dating); Actor2 = getSpecific(Dating); If Actor.isInterestedIn(Actor2) && Actor2.isInterestedIn(Actor)
OTHER: isModEnabled(vin_Polyamorous)

    SceneStart()
    "J'allais à mes occupations lorsque tout d'un coup je me fais interrompre en entendant quelqu'un jouir, ça semble venir de la chambre de <Actor.name>. Allons voir!"
    Sex(Actor, Actor2)
    "Mes deux amants s'amusent sans moi. Je suis sûr que cela ne les dérangerait pas si j'allais les rejoindre!"
    0:: "Les rejoindre"
    1:: "Non"
    
    If 0
        "Cela fait partie du plaisir d'une relation polyamoureuse, non!"
        Sex(Player, Actor, Actor2)
    Endif 
    SceneEnd()
    timeout(200, cms_two_datings)