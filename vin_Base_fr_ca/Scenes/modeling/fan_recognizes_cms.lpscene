WHAT: all
WHERE: all, -home, -work, -prison
WHEN: 0 - 24
WHO: none
OTHER: Random(1, 100) < modelfame || Random(1, 100) < pornfame

    SceneStart()
    Actor = generatePersonTemporary()
    Actor.dress()
    Actor.show()
    Actor:attractiontoplayer += 50
    Actor:rapportwithplayer += 50
    
    "J'ai été arrêté par un fan et me demande de prendre une photo. <Actor.Il_or_Elle> se nomme <Actor.name>"
    0:: "Prendre une photo avec le fan"
    1:: "L'ignorer et m'éloigner rapidement"
    
    If 0
        "Ce sont les fans qui font avancer ma carrière après tout, donc ce n'est pas trop demander de prendre des selfies occasionnels avec eux."
        0:: "Demander le numéro de tel. du fan"
        1:: "Dire au revoir"
        
        If 0
            "Le fan a été absolument frappé par le fait que je lui demande son numéro de tel. et bien sûr que <Actor.il_or_elle> me l'a donné sans hésitation."
            Actor.makePermanent()
            exchangeContact(Actor)
        Endif
    Endif
    SceneEnd()
    timeout(200, fan_recognizes_cms)