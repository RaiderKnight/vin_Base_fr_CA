WHAT: all
WHERE: university:school, university:work
WHEN: 0-24
WHO: Actor = getSpecific(Lecturer); Actor2 = getSpecific(Coursemate); If !Actor.isDating() && !Actor2.isDating() && Actor2.isInterestedIn(Actor) && Actor.isInterestedIn(Actor2) && Actor:perversion + Actor2:perversion > Random(75, 150)
OTHER: none

    SceneStart()
    Actor2.dress()
    Actor2.show(3)
    dressFormal()
    Actor.dress()
    Actor.show(2)

    If WHERE == university:school
        "Attend un peu, qu'est-ce que mon camarade de cours <Actor2.name> et le Dr <Actor.name_last> font ensemble, ils s'en vont vers le bureau de Dr <Actor.name_last>? Bizarre, Dr <Actor.name_last> n'est pas supposé rencontrer des étudiants après les cours..."
    Else
        "Attend un peu, qu'est-ce que mon élève <Actor2.name> et le Dr <Actor.name_last> font ensemble, ils s'en vont vers le bureau de Dr <Actor.name_last>? Bizarre, Dr <Actor.name_last> n'est pas supposé rencontrer des étudiants après les cours..."
    Endif
    "Que dois-je faire?"
    0:: "Les suivres"
    1:: "Je m'en fou!"
    
    If 0
        If WHERE == university:school
            "Pourraient-ils y avoir une liaison entre les deux? J'imagine que <Actor2.name> veut coucher avec <Actor.name> pour faire monter ces notes... Vège!"
        Else
            "Pourraient-ils y avoir une liaison entre les deux? Baiser avec ces élèves c'est vraiment dégueux, <Actor.name> a vraiment baisser dans mon estime, je <Actor2.le_or_la> pensais pas comme ça!"
        Endif
        "Que dois-je faire maintenant?"
        0:: "Espionner"
        1:: "Qu'ils fassent ce qu'ils veulent!"
        
        If 0
            SneakGame()
            If Random(0, 100) < sneak
                "Heureusement, ils ont mal fermer la porte derrière eux, elle est entreouverte, me laissant la chanche de regarder au travers de l'ouverture pour jeter un coup d'œil."
                Sex(Actor, Actor2)
                Actor.hide()
                Actor2.hide()
                Player.show(0)
            Else
                "Malheureusement, ils ont verrouillé la porte dès qu'ils sont entrés dans le bureau et je n'ai donc pas eu la chance de voir quoi que ce soit."
            Endif
        Endif
        
        "Maintenant, que dois-je faire à propos de ce que j'ai vu?"
        0:: "Le dire à tout le monde"
        1:: "Ne rien dire"
        
        If 0
            karma -= 10
            If WHERE == university:school
                "J'ai décidé de le dire à tous les élèves de mon cours, et sans surprise, <Actor2.name> a fermement nié le tout."
            Else
                "J'ai décidé de le dire à tous mes collègues professeurs, et sans surprise, <Actor.name> a fermement nié le tout."        
            Endif
            Actor:rapportwithplayer -= Random(0, 5)
            Actor2:rapportwithplayer -= Random(0, 10)
        Else
            If WHERE == university:school
                "Soyons honnêtes: je me tapperais totalement un professeur moi-même si j'en avais l'occasion. J'envie <Actor2.name>, j'aimerais ça être à sa place. Ne détruisons pas la réputation et la carrière du Dr <Actor.name_last>."
            Else
                "Soyons honnêtes: je me tapperais totalement un étudiant moi-même si j'en avais la chance. J'envie <Actor.name>, j'aimerais ça être à sa place. Ne détruisons pas la réputation et la carrière du Dr <Actor.name_last>."            
            Endif
        Endif
    Else
        "Bof, je me fais surement des idées, <Actor.name> demande probablement simplement de l'aide pour ces cours."
    Endif
    
    SceneEnd()
    timeout(500, coursemate_affair)