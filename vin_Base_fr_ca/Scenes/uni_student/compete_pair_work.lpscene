WHAT: attend_lectures
WHERE: university:school
WHEN: 0-24
WHO: Actor = getSpecific(Coursemate); Actor2 = getSpecific(Coursemate); If Actor:rapportwithplayer > 0 && Actor2:rapportwithplayer > 0
OTHER: Random(0, 100) < intelligence

    SceneStart()
    Actor.dress()
    Actor.show(2)
    Actor2.dress()
    Actor2.show(3)
    "Le professeur vient de nous remettre un devoir, à faire en groupe de 2. Deux de mes camarades de classe <Actor.name> et <Actor2.name>, me demandent tous les deux de travailler avec eux ..." 
    "Qui choisir ?"
    0:: "Travailler avec <Actor.name>"
    1:: "Travailler avec <Actor2.name>"
    
    If 0
        Actor:rapportwithplayer += Random(0, 2)
        Actor2:rapportwithplayer -= Random(0, 2)
    Else
        Actor2:rapportwithplayer += Random(0, 2)
        Actor:rapportwithplayer -= Random(0, 2)    
    Endif
    SceneEnd()
    timeout(200, compete_pair_work)