WHAT: all
WHERE: university:school, university:work
WHEN: 0-24
WHO: Actor = getSpecific(Lecturer); Actor2 = getSpecific(Coursemate); If !Actor.isDating() && !Actor2.isDating() && Actor2.isInterestedIn(Actor) && Actor2:rapportwithplayer < 50
OTHER: none

    SceneStart()
    Actor2.dress()
    Actor2.show(3)
    dressFormal()
    Actor.dress()
    Actor.show(2)

    If WHERE == university:school
        "Attend un peu, est-ce que <Actor.mon_or_ma> camarade de classe <Actor2.name> et le Dr <Actor.name_last> retournent ensemble à son bureau ?"
    Else
        "Attend un peu, est-ce que c'est mon élève <Actor2.name> avec le Dr <Actor.name_last> qui retournent ensemble à son bureau?"
    Endif
    "Que dois-je faire?"
    0:: "Répandre une rumeur"
    1:: "No big deal"
    
    If 0
        If WHERE == university:school
            "Ils ont peut-être une liaison ensemble, faire du cul avec un prof me semble que c'est vège! Ont peu déjà savoir ce qu'<Actor2.il_or_elle> va faire plus tard comme job!"
            "Entoutka c'est peut-être pas le cas mais, j'ai quand même décidé de répandre une rumeur dans la classe, comme quoi ils couchent ensemble, ce que <Actor2.name> a fermement nié."
        Else
            "Ils ont peut-être une aventure ensemble, faire du cul avec un élève me semble que c'est pervers! Si <Actor.name> ce fait prendre, il perd sa job c'est clair!"
            "Entoutka c'est peut-être pas le cas mais, j'ai quand même décidé de répandre une rumeur dans la café des profs, ce que <Actor.name> a fermement nié."        
        Endif
        Actor:rapportwithplayer -= Random(0, 2.5)
        Actor2:rapportwithplayer -= Random(0, 5)
    Else
        "Bof, je m'en fou! <Actor.name> demande peut-être juste de l'aide pour ces cours."
    Endif
    
    SceneEnd()
    timeout(200, coursemate_gossip)