WHAT: all
WHERE: university:school, university:work
WHEN: 0-24
WHO: Actor = getSpecific(Coursemate); Actor2 = getSpecific(Coursemate); If !Actor.isDating() && !Actor2.isDating() && Actor.isInterestedIn(Actor2) && Actor2.isInterestedIn(Actor) && !Actor.hasRelationship(Spouses, Dating) && !Actor2.hasRelationship(Spouses, Dating) && Actor:attractiontoplayer < 40 && Actor2:attractiontoplayer < 40
OTHER: none

    SceneStart()
    Actor.dress()
    Actor.show(2)
    Actor2.dress()
    Actor2.show(3)
    If WHERE == university:school
        "Deux de mes camarades de classe <Actor.name> et <Actor2.name> ont été vus ensemble ces jours-ci, hors classe ... Je me demande s'ils couchent ensemble ..."
    Else
        "Deux de mes élèves <Actor.name> et <Actor2.name> ont été vus ensemble ces jours-ci, hors classe ... Je me demande s'ils couchent ensemble ..."        
    Endif
    "Comment devrais-je me sentir à propos de ça?"
    0:: "Tant mieux pour eux"
    1:: "C'est dommage"

    addNpcRelationship(Dating, Actor, Actor2)
    SceneEnd()
    timeout(1000, coursemates_dating)