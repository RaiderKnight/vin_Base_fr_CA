WHAT: all
WHERE: home
WHEN: 8 - 20
WHO: Actor = getSpecific(Landlord); If Actor:perversion > 80 && Actor.isInterestedIn(Player)
OTHER: isAtHome()

    SceneStart()
    Actor2 = getSpecific(Dating)
    Actor.dress()
    Actor.show()
    "C'est quand déjà que le propriétaire vient chercher le loyer, aujourd'hui je pense, <Actor2.le_or_la> pervers offre une grosse réduction si je couche avec <Actor2.lui_or_elle> ..."
    0:: "Ok ..."
    1:: Actor2.isValid() && Actor.isInterestedIn(Actor2):: "Offrir <Actor2.name> à la place"
    2:: "T'es tu malade, non!"
    
    If 2
        "De quel genre d'offre s'agit-il?"
    Else
        If 0
            Sex(Player, Actor)
        Else
            Sex(Actor, Actor2)
        Endif
        money += 500
    Endif
    SceneEnd()
    timeout(1000, cms_ica_sep05)