WHAT: work_hourly
WHERE: restaurant, cafe, biergarten, pub
WHEN: 0 - 24
WHO: none
OTHER:

    SceneStart()
    "Mon lieu de travail propose un menu de livraison. On m'a demandé d'aller livrer une commande à emporter ... Ce n'est pas ma partie préférée de mon travail, mais cela doit être fait."
    setBackgroundCustom(livingroom)
    Actor = generatePersonTemporary()
    If Random(0, 100) < Actor:perversion
        Actor.show()
        Actor.makeInterested(Player)
        Actor:attractiontoplayer => 30
        "J'arrive chez le client, cogne à sa porte..."
        "Le client ouvre la porte pour recevoir sa livraison, mais <Actor.il_or_elle> est complètement nu!"
        Actor:: "Tu sais ... Si tu veux, tu peux rester un petit mmoment avec moi manger... c'est un peu platte manger tout seul. Ça te tente-tu me tenir compagnie pendant un moment?"
        0:: "Ok mais, c'est autre chose que je veux manger..."
        1:: "Donne-moi le cash avant que j'appel la police"
        
        If 0
            Sex(Player, Actor)
        Endif
    Else
        Actor.dress()
        Actor.show()
        "C'était juste une livraison habituelle et j'ai même reçu un très bon pourboire."
        money += 5
    Endif
    SceneEnd()
    timeout(50, waiter_delivers)