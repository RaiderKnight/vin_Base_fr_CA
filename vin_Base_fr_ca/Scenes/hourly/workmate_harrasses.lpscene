WHAT: work_hourly
WHERE: all
WHEN: 0 - 24
WHO: none
OTHER: Random(30, 1000) < attractiveness

    SceneStart()
    "J'attire beaucoup d'attention au travail par ma beauté. Un de mes collègues est un fucking pervers et me lâche pas d'une semelle ces temps-ci."
    0:: "Me plaindre au boss"
    1:: "L'ignorer"
    
    If 0
        masochist -= Random(0, 0.25)
        "J'ai tout raconté au patron au sujet de mon collègue pervers, l'accusant de harcèlement sexuel. <Player.Il_or_Elle> n'a pas été renvoyé mais a reçu un avertissement, assez pour <Player.le_or_la> mettre loins de moi, au moins pour le moment."
    Else
        "C'est juste un flirt inoffensif au travail. En plus, j'apprécie quand même ces commentaires cochons."
        masochist += Random(0, 0.25)
        perversion += Random(0, 0.1)
    Endif
    SceneEnd()
    timeout(100, workmate_harasses)