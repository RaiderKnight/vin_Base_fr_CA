WHAT: work_hourly
WHERE: bar, nightclub, pub, biergarten
WHEN: 0 - 24
WHO: none
OTHER:

    SceneStart()

    Actor = generatePersonTemporary()
    While !Actor.isInterestedIn(Player)
        Actor = generatePersonTemporary()
    Endwhile
    Actor.dress()
    Actor.show()
    "L'un des clients que je sers semble clairement attiré par moi et essaie vraiment d'engager une conversation."
    If Random(0, 100) < Actor:perversion
        Actor:: "Tu me rends tellement fou, bébé. Pourquoi ne pas venir à la maison avec moi après ta job? Tu vas voir ont va avoir du fun!"
        0:: "Fuck the customer"
        1:: "Appeler le bouncer"
        
        If 0
            setBackground(home)
            Sex(Player, Actor)
            money += 200
        Else
            "Ok, je pense que tu as assez bu! Je ne suis pas une poupée gonflable...! Je suis immédiatement allé voir le bouncer, qui a demandé avec force au client de partir."
        Endif
    Else
        "Heureusement, le client s'est bien comporté et ne m'a pas harcelé de quelque façon que ce soit, même en état d'ébriété, juste un flirt inoffensif ici et là. <Actor.Il_or_Elle> m'a même laissé un gros pourboire."
        money += 10
    Endif
    SceneEnd()
    timeout(50, bartender_frisky)