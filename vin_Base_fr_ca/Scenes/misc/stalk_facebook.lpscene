WHAT: none
WHERE: none
WHEN: 0 - 24 
WHO: none
OTHER: none

    SceneStart()
    If isTimingOut(stalk_facebook)
        "Je n'ai pas envie de faire plus de recherche sur les réseaux sociaux pour le moment ... Peut-être que je serai de nouveau d'humeur dans une heure ou deux."
    Else
        Actor = getSpecific(Impregnated)
        "Dois-je essayer de rechercher <Actor.name> en ligne? La première fois que nous nous sommes rencontrés, je n'ai pas tout à fait réussi à obtenir son numéro."
        0:: "Oui"
        1:: "Non"
        
        If 0
            intelligence += Random(0, 0.1)
            If Random(0, 100) < intelligence
                "Ah, j'ai trouvé <Actor.name>!"
                Actor.dress()
                Actor.show(2)
                "Envoyons une demande d'ami à <Actor.name>."
                "Une heure plus tard ..."
                If Random(0, 75) < Actor:attractiontoplayer || Random(0, 75) < Actor:rapportwithplayer || Actor:intelligence < Random(0, 50)
                    "<Actor.name> a accepté ma demande d'amitié sur Facebook. Je savais que j'avais laissé une bonne première impression!"
                    exchangeContact(Actor)
                Else
                    "Toujours pas de réponse ... <Actor.Il_or_Elle> a probablement ignoré ma demande."
                Endif
            Else
                "Merde, j'ai passé des heures à essayer de trouver <Actor.name> parmi les milliards d'utilisateurs de Facebook, en vain. Je pense que je vais abandonner."
            Endif
            timeout(2, stalk_facebook)
        Endif
    Endif
    
    SceneEnd()