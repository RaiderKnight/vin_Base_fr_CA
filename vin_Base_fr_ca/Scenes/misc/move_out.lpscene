WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    If isAtHome()
        "Suis-je sûr de vouloir quitter mon maison actuelle?"
        0:: "Oui"
        1:: "Non"
        
        If 0
            Rent = getRent()
            If Rent > 0
                "Cet appartement n'était tout simplement pas pour moi. J'ai décidé de résilier mon contrat de bail avec le propriétaire."
            Else
                Difficulty = Random(0.25, 2)
                Salary = getSalary()
                Rent = Random(800, 1500)*CostOfLiving+Difficulty*Salary*0.25
                Price = Rent*120
                PriceConverted = Price.convertToLocalCurrency(true)
                "J'ai réussi à vendre ma maison pour <PriceConverted>."
                money => money + Price
            Endif
            "Il est temps de trouver un nouvel endroit où vivre."
            loseHome()
        Else
            "Après réflexion, je ne déteste pas tellement cet endroit."
        Endif
    Else
        "Je ne loue pas cette maison! Il n'y a donc pas de contrat de bail à résilier."
    Endif
    
    SceneEnd()