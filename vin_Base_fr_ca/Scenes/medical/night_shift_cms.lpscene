WHAT: all
WHERE: hospital:work
WHEN: 18 - 4
WHO: Actor = getSpecific(Doctor, Nurse); If Actor.isInterestedIn(Player) && Player.isInterested(Actor) && Actor:perversion > 70 && Actor:attractiontoplayer > 20
OTHER:

    SceneStart()
    Actor.dressUniform()
    Actor.show()
    "Je suis dans l'équipe de nuit aujourd'hui, ce qui s'est déroulé sans incident et ennuis. Au moins, <Actor.name> est là pour me tenir compagnie."
    Actor:: "C'est trop platte en ce moment, il se passe rien ! Tu sais quoi, puisque nous sommes seuls ici ... allons gosser un peu, ché pas, ont pourraient baiser question de passer le temps ..."
    "Woah, c'est une proposition directe ..."
    0:: "Ok, drette ici !"
    1:: "Non, pas vraiment mon genre!"
    
    If 0
        "C'est vrai que c'est une bonne façon de passer le temps ... Ont devraient le faire plus souvent..."
        Sex(Player, Actor)
        passTime(1, 3)
    Endif
    SceneEnd()
    timeout(200, night_shift_cms)