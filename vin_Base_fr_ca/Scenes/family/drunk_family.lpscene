WHAT: all
WHERE: nightclub, bar
WHEN: 21 - 3
WHO: Actor = getRelativeExcept(GrandParent); If Random(70, 500) < Actor:interpersonal
OTHER: none

    SceneStart()
    Actor.dress()
    Actor.show(2)

    "Il y a <Actor.un_or_une> <Actor.gars_or_fille> complètement <Actor.saoul_or_saoulle> qui vomit sur le sol. Que veux-tu, il y en a qu'ils ne savent pas boire!"
    "Oups! ... c'est <Player.ma_or_mon> <Actor.relationship>! Pas fooort ..."
    0:: "Ignorer"
    1:: "Engueuler <Actor.name>"
    2:: "<Actor.La_or_Le> reconduire à la maison"

    If 0
        "Je vais juste faire semblant de ne pas <Actor.le_or_la> connaître et ce ne sont pas mes affaires. Le personnel va nettoyer ça."
    Elseif 1
        Player(Angry):: "Hey <Actor.le_or_la> <Actor.grand_or_grande>! Quand on sait pas boire , on boit pas, en passant!"
        karma -= 2.5
        masochist -= Random(0, 0.5)
        Actor:rapportwithplayer -= Random(0, 5)
    Else
        "En tant que <Actor.callplayer>, je ne peux pas vraiment laisser <Actor.name> comme ça. Je vais devoir <Actor.le_or_la> ramener à la maison."
        karma += 2.5
        Actor:rapportwithplayer += Random(0, 5)
    Endif

    SceneEnd()
    timeout(500, drunk_family)