WHAT: all, -sleep, -nap, -sleep_hotel, -nap_hotel
WHERE: all
WHEN: 16 - 21
WHO: Actor = getSpecific(Dating); Actor2 = getRelative(); If !Actor.hasRelationship(Spouses) && Random(20, 500) < Actor:attractiontoplayer && Actor2:rapportwithplayer > -10 && !Actor.isExRelative()
OTHER: !isPlayerMarried()

    SceneStart()
    secondScreenIfHidden(Actor)
    Actor.dress()
    Actor.show(2)
    "Je sors avec <Actor.name> depuis un certain temps et jusqu'à présent, il semble que nous pourrions avoir un avenir ensemble. Peut-être que je devrais commencer à présenter progressivement <Actor.name> à ma famille. Nous avons un dîner en famille bientôt, je devrais peut-être l'emmener avec moi?"
    0:: "Oui"
    1:: "Non"
    
    If 0
        "C'est une occasion parfaite de montrer à quel point je suis engagé dans ma relation."
        karma += 2.5
        secondScreen()
        Loc = findNearbyBuilding(restaurant)
        moveTo(Loc)        
        "Quelques jours plus tard ..."
        Actor.show()
        Actor2.dress()
        Actor2.show(3)        
        Player(Embarassed):: "<Actor2.Relationship>, voici <Actor.mon_or_ma> <Actor.chum_or_blonde>, <Actor.name>."
        Actor(Happy):: "Ravi de vous rencontrer enfin en personne. <Player.name> n'a jamais cessé de dire à quel point vous êtes incroyable."
        Actor2(Happy):: "Ravi de vous rencontrer aussi, <Actor.name>. Vous êtes aussi un joli couple."
        Actor:attractiontoplayer += Random(0, 3)
        Actor2:rapportwithplayer += Random(0, 2)
    Else
        "Non, il est trop tôt dans notre relation pour faire ça. On pourrait rompre à tout moment. Comment pourrais-je expliquer ça à ma famille alors?"
        Actor:attractiontoplayer -= Random(0, 1)
    Endif
    SceneEnd()
    timeout(1000, dating_family_dinner)