WHAT: all, -sleep, -nap
WHERE: home
WHEN: 17 - 21
WHO: Actor = getRelative(Parent, StepParent); Actor2 = getRelative(Sibling, StepSibling); If Random(10, 300) < Actor2:intelligence - Player:intelligence
OTHER: jobexperience < 40 && isAtHome()

    SceneStart()
    "J'étais à un dîner de famille lorsque le sujet des carrières a été abordé."
    Actor.dress()
    Actor.show(2)
    Actor2.dress()
    Actor2.show(3)
    Actor(Happy):: "<Player.name>, as-tu entendu dire que <Actor2.name> vient de recevoir une promotion au travail? En attendant, regarde-toi - es-tu toujours à ce poste avec un salaire minable et sans perspectives? Quand va-tu mettre de l'ordre dans votre vie et faire comme <Actor2.name>?"
    0:: "Être sur la défensive"
    1:: "Féliciter <Actor2.name>"
    
    If 0
        Player(Angry):: "Il n'y a rien de mal dans mon travail. J'aime ça. Tu sais qu'il y a d'autres objectifs dans la vie que l'argent, il n'y a pas de sous métier?"
        Actor:rapportwithplayer -= Random(0, 2)
        Actor2:rapportwithplayer -= Random(0, 2)
        masochist -= Random(0, 0.25)
    Else
        Player(Excited):: "Wow, félicitations, <Actor2.name>. Je suis fier de toi! J'espère que tu vas garder ton poste longtemps!"
        Actor2:rapportwithplayer += Random(0, 2)
        masochist += Random(0, 0.25)
        karma += 1
    Endif
    SceneEnd()
    timeout(200, siblings_compared)
