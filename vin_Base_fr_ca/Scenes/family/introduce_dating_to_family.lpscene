WHAT: all, -sleep, -nap, -sleep_hotel, -nap_hotel
WHERE: all
WHEN: 0 - 24
WHO: Actor = getSpecific(Dating); Actor2 = getRelative(); If !Actor.hasRelationship(Spouses) && Random(20, 500) < Actor:attractiontoplayer && Actor2:rapportwithplayer > -10 && !Actor.isExRelative()
OTHER: !isPlayerMarried()

    SceneStart()
    Actor.dress()
    Actor.show(2)
    "Je sors avec <Actor.name> depuis un moment et jusqu'à présent, il semble que nous pourrions avoir un avenir ensemble. Peut-être que je devrais commencer progressivement à présenter <Actor.name> à ma famille. Je vois ma famille aujourd'hui, je devrais peut-être emmener <Actor.name> avec moi?"
    0:: "Oui"
    1:: "Non"
    
    If 0
        "C'est une occasion parfaite de montrer à quel point je suis engagé dans ma relation."
        karma += 2.5
        Loc = Actor2.getBuilding(home)
        moveTo(Loc)
        "Plus tard nous arrivons chez ma famille ..."
        Actor2.dress()
        Actor2.show(3)        
        Player(Embarassed):: "<Actor2.name>, voici mon <Actor.frere_or_soeur>, <Actor.name>."
        Actor (Happy) :: "Ravi de vous rencontrer enfin en personne. <Player.name> me parle toujours de toi et comment tu es incroyable."
        Actor2(Happy):: "Ravi de vous rencontrer aussi, <Actor.name>."
        Actor:attractiontoplayer += Random(0, 3)
        Actor2:rapportwithplayer += Random(0, 2)
    Else
        "Non, il est trop tôt dans ma relation pour faire ça. Nous pourrions rompre à tout moment. Comment pourrais-je expliquer cela à ma famille...?"
        Actor:attractiontoplayer -= Random(0, 1)
    Endif
    SceneEnd()
    timeout(1000, introduce_dating_to_family)