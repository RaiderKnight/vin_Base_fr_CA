WHAT: all, -sleep, -nap
WHERE: home
WHEN: 0 - 24
WHO: Dating = getRelative(); If Dating.LivesWithPlayer() && Dating:perversion > Random(70, 100)
OTHER: !isWithCompanion() && isAtHome() && Random(0, 1000) < intelligence

    SceneStart()

    "Par accident, j'ai découvert que <Dating.name> s'est inscrit sur un site de webcam pour adultes en live! En fait, je suis presque sûr que c'est l'un de ces passe-temps préférés de quand je ne suis pas là."
    "Je réagi comment à cette découverte?"
    0:: "<Dating.Il_or_Elle> fait mal à personne"
    1:: "L'embarasser"

    If 0
        "Quel est le problème? <Dating.Il_or_Elle> a juste une libido saine et doit se libérer de temps en temps. Regarder une <Player.gars_or_fille> nue en train de se masturber sur sa webcam est inoffensif - j'appelle pas cela me tromper!"
        perversion += Random(0, 1)
        masochist += Random(0, 0.5)
    Else
        "Je me suis énervé et je lui ai fait savoir que c'était embarrassant et inacceptable et qu'<Dating.il_or_elle> devait arrêter ou qu'<Dating.il_or_elle> déménage."
        Dating:rapportwithplayer -= Random(0, 2)
        Dating:perversion -= Random(0, 1)
    Endif

    timeout(200, family_webcamshow)
    SceneEnd()