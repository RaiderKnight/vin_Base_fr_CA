WHAT: all, -sleep, -nap
WHERE: home
WHEN: 9 - 20
WHO: Actor = getRelative(); If !Actor.livesWithPlayer() && Random(20, 100) < Actor:rapportwithplayer && Actor:incest == 0
OTHER: isAtHome()

    SceneStart()
    Actor.dress()
    Actor.show(2)
    Actor(Happy):: Comment vas-tu, mon cher <Actor.callplayer>? Je viens te voir pour te rendre visite. Ça fait un moment que je ne t'ai pas vu!"
    "<Actor.name> m'a rendu une visite surprise. Comme c'est gentil de sa part! La vie peut être difficile mais c'est réconfortant de savoir que ma famille est toujours juste derrière moi."
    Actor:rapportwithplayer += Random(0, 1)
    mood += Random(0, 10)
    
    SceneEnd()
    timeout(96, family_visit)