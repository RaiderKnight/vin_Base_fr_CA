WHAT: all, -sleep, -nap, -sleep_hotel, -nap_hotel
WHERE: all
WHEN: 16 - 2
WHO: Actor2 = getSpecific(Prostitute); If Random(0, 250)*Random(0, 250) < Actor2:attractiveness*Actor2:attractiveness
OTHER: !isWithCompanion()

    SceneStart()
    Dice = Random(100, 500) + pornfame*5
    P1C = Dice.convertToLocalCurrency(true)
    P2 = Dice/2
    P2C = P2.convertToLocalCurrency(true)
    P3 = Dice/3
    P3C = P3.convertToLocalCurrency(true)
    Actor = generatePersonTemporary()
    Actor.dress()
    Actor.show(2)
    "Une personne m'a envoyé un message sur Adultwork pour savoir si je voulais baiser ..."
    Actor(Flirty):: "Tu charge combien pour avoir du fun avec <Actor2.name>?"
    0:: "Refuser l'invitation"
    1:: "Les voler (<P1C>)"
    2:: "Offrir un prix juste (<P2C>)"
    3:: "Offrir un bargain (<P3C>)"
    
    If 0
        Player(Sad):: "Désolé, mais <Actor2.name> n'est pas disponible en ce moment ..."
    Elseif choice*Actor2:attractiveness > Actor:intelligence + Actor:interpersonal
        Actor(Flirty):: "On dirait que nous avons un accord."
        "Aussitôt, nous nous sommes rencontrés dans une chambre de motel sur laquelle nous nous sommes mis d'accord."
        Actor2.dress()
        Actor2.show(1)
        setBackground(home)
        Dice => Dice*0.5
        money => money + Dice/choice
        Actor3:perversion += 0.25
        0:: "Les laisser seuls"
        1:: "Regarder le show"
        
        If 1
            Sex(Actor, Actor2)
        Endif
    Else
        Actor(Angry):: "Pas question, c'est trop ben trop cher! T'es pas l'escorte du siècle pour demander un prix si haut. Tu devrais regarder les profils des autres escortes et voir combien ils facturent. Tu vas voir que tu es dans le champ!"
    Endif
    
    SceneEnd()
    timeout(3, john_online_pimp, john_threesome_online_pimp)