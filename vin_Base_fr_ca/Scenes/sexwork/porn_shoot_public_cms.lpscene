WHAT: all
WHERE: all, -work
WHEN: 8 - 18
WHO: none
OTHER: Random(1, 150) < pornfame

    SceneStart()
    Dice = Random(500, 2000)
    P1C = Dice.convertToLocalCurrency(true)
    P2 = Dice/2
    P2C = P2.convertToLocalCurrency(true)
    P3 = Dice/3
    P3C = P3.convertToLocalCurrency(true)
    Actor = generatePersonTemporary()
    While !Player.isInterestedIn(Actor)
        Actor = generatePersonTemporary()
    Endwhile
    Actor.dress()
    Actor.show(2)
    "Un réalisateur porno veut me réserver pour une scène de sexe avec <Actor.name> ..."
    "Cette scène sera tournée en public, avec des faux spectateurs embauchés pour cacher la scène aux autorités compétentes." 
    0 :: "Refuser l'offre"
    1 :: "Voler le directeur (<P1C>)"
    2 :: "Offrir un prix juste (<P2C>)"
    3 :: "Offrir un prix réduit (<P3C>)"
    
    If 0
        Player(Sad):: "Désolé, mais j'ai quelque chose de prévue cette semaine-là ..."
    Elseif 3 || Random(0, 100) < choice*pornfame
        "Le réalisateur porno a accepté mon offre et assez vite nous nous sommes rencontrés sur le lieu de tournage sur lequel nous nous sommes mis d'accord."
        setBackground(street)
        Actor.show()
        Actor2 = generatePersonTemporary()
        Actor3 = generatePersonTemporary()
        Actor2.dress()
        Actor2.show()
        Actor3.show()
        Actor3.dress()
        "Bon sang, il y a pas mal de spectateurs ici. Je suppose qu'on aurait pu m'avertir d'avance."
        Sex(Actor, Player)
        money => money + Dice/choice
        pornfame += 0.5
        perversion += 0.25
        "Les spectateurs ont l'air très enthousiasmés par ma performance, certains d'entre eux demandent de se joindre à la fête ..."
        0:: "Baiser aussi avec deux spectateurs"
        1:: "Ne fait pas partie de l'accord"
        
        If 0
            "Les fans de porno adorent voir des gens en manquent de sexe qui baisent avec n'importe qui! Du contenu parfait pour faire de la pub et augmenter ma popularité."
            Sex(Player, Actor2, Actor3)
            pornfame += 2
        Endif
    Else
        "Pas question, c'est bien au-dessus du taux du marché pour ce type de tournage! Vous avez une opinion trop élevée de vous-même, chérie. Vous devriez demander aux autres stars du porn combien elles chargent."
    Endif
    
    SceneEnd()
    timeout(200, porn_shoot_public_cms)