WHAT: all, -sleep, -nap
WHERE: home
WHEN: 0 - 24
WHO: Actor = getSpecific(Dating); If Actor:perversion < 50 && !Actor.isProstitute() && Actor:masochist < 25
OTHER: Random(0, 1000) < pornfame/intelligence

    SceneStart()
    Actor.dress()
    Actor.show(2)
    Actor(Angry):: "<Player.name>, pouvons-nous parler s'il te plaît?"
    "Oh merde, ça ne sonne pas bien."
    Actor(Sarcastic):: "Comme ça tu t'amuse avec quelqu'un depuis un bout? Tu m'as caché la vérité tout ce temps."
    "<Actor.name> me montre une capture d'écran ... C'est mon compte Twitter porno ..."
    Actor(Angry):: "C'est fini! Je romps avec toi. C'est la plus grande humiliation de ma vie, être avec une star du porno sans le savoir. C'est quoi mes proches vont penser?"
    "Inutile d'essayer d'en parler à <Actor.name> maintenant ... ça ne sert à rien. Peu de gens peuvent tolérer que leur autre significatif couche avec une autre personne, encore moins le faire pour de l'argent et le publier partout sur Internet ou en films."
    If isPlayerMarried()
        Actor(Sad):: "J'ai peur que ce mariage ne fonctionne plus ..."
        "Et juste comme ça, <Actor.name> avons divorcé ..."
        mood -= 100
        Divorce()
        LoseDating()
        If Player.isDominantSex(Actor) && money > 1000
            "Pour aggraver les choses, le tribunal a accordé la moitié de ma fortune durement gagnée à <Actor.name> ... Quel désastre de mariage c'était ..."
            money -= money*0.5
        Endif        
    Else
        "Et juste comme ça, <Actor.name> a rompu avec moi ... De plus, j'ai découvert plus tard qu'<Actor.il_or_elle> avait également bloqué mon numéro."
        LoseDating()
        mood -= 50
    Endif
    blockContact(Actor)
    Actor:attractiontoplayer -= Random(0, 100)
    SceneEnd()