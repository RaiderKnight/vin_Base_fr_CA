WHAT: do_cardio, lift_weights
WHERE: sports_centre
WHEN: 0 - 24
WHO: none
OTHER: Random(0, 100) < 5

SceneStart()
Actor = generatePerson()
Actor.dress()
Actor.show(2)

Actor(Furious):: "Argh!"
"Il y a <Actor.un_or_une> <Actor.gars_or_fille> qui soulève des poids et qui grogne et hurle depuis 10 minutes. <Actor.Il_or_Elle> ne soulève même pas grand chose, genre 100lb!"
0:: "L'ignorer"
1:: "Lui laisser un commentaire"

If 0
    "Ce ne sont pas de mes affaires. Certaines personnes aiment juste un peu d'attention, je suppose."
    masochist += Random(0, 0.5)
Else
    Player(Sarcastic):: "Hey <Actor.le_or_ma> <Actor.gros_or_belle>, crier ne t'aideras pas à soulever plus de poids!"
    masochist -= Random(0, 0.5)
    Actor:rapportwithplayer -= Random(0, 5)
Endif

SceneEnd()
timeout(24, grunt_and_scream)