WHAT: do_cardio, lift_weights
WHERE: sports_centre
WHEN: 0 - 24
WHO: none
OTHER: Random(40, 300) < muscle

SceneStart()
Actor = generatePerson()
Actor.dress()
Actor.show(2)

"Je n'ai pas pu m'empêcher de remarquer qu'<Actor.un_or_une> <Actor.gars_or_fille> avait une technique terrible avec l'un des ascenseurs. <Actor.Il_or_Elle> va se blesser un de ces jours ..."
0:: "L'ignorer"
1:: "L'aider dans sa technique"

If 0
    "Ce ne sont pas mes affaires. De plus, je ne veux pas passer pour un ti-joe connaissant."
    masochist += Random(0, 0.5)
Else
    "J'ai décidé de donner à <Actor.name> quelques conseils pour améliorer sa technique et <Actor.le_or_la> protéger des blessures."
    masochist -= Random(0, 0.5)
    If Random(0, 100) < Actor:muscle
        Actor:rapportwithplayer -= Random(0, 5)
        "Cependant, <Actor.il_or_elle> a souri à mes conseils et n'a pas semblé écouter beaucoup."
    Else
        Actor:rapportwithplayer += Random(0, 5)
        "<Actor.Il_or_Elle> m'a remercié de l'avoir averti de sa mauvaise technique et a suivi mes conseils."
        karma += 2.5
    Endif
Endif

SceneEnd()
timeout(24, wrong_form)