WHAT: do_cardio, lift_weights
WHERE: sports_centre
WHEN: 0 - 24
WHO: none
OTHER: Random(0, 100) < 5

SceneStart()
Actor = generatePerson()
Actor.dress()
Actor.show(2)

"Je n'ai pas pu m'empêcher de remarquer qu'<Actor.un_or_une> <Actor.gars_or_fille> travaillant dans la section des haltères, admirant ces muscles et courbes dans le miroir. Sauf que, cela bloque l'accès aux autres haltères."
0:: "L'ignorer"
1:: "Lui dire de se tasser"

If 0
    "Ce ne sont pas mes affaires. De plus, c'est compréhensible compte tenu de l'ère des médias sociaux dans laquelle nous vivons."
    masochist += Random(0, 0.5)
Else
    Player(Angry):: "Hey <Actor.le_or_ma> <Actor.gros_or_chère>! Tu es dans le chemin de tout le monde. Fais juste te tasser un peu, tout le monde va être content!"
    masochist -= Random(0, 0.5)
    Actor:rapportwithplayer -= Random(0, 5)
Endif

SceneEnd()
timeout(24, mirror_magnet)