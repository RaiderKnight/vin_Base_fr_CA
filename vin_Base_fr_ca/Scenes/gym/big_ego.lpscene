WHAT: do_cardio, lift_weights
WHERE: sports_centre
WHEN: 0 - 24
WHO: none
OTHER: Random(0, 100) < 5

SceneStart()
Actor = generatePerson()
Actor.dress()
Actor.show(2)

"Je n'ai pas pu m'empêcher de remarquer <Actor.un_or_une> <Actor.gars_or_fille> qui mett beaucoup trop de poids sur sa barre et qui fait à peine des demi-répétitions avec une technique pénible à regarder."
0:: "L'ignorer"
1:: "Lui lancer un commentaire"

If 0
    "Ce ne sont pas mes affaires. De plus, <Actor.il_or_elle> a juste un gros ego et veut juste le montrer un peu."
    masochist += Random(0, 0.5)
Else
    Player(Angry):: "Tu soulève ben trop lourd pour ta grosseur. Laisse ton égo chez vous!"
    masochist -= Random(0, 0.5)
    Actor:rapportwithplayer -= Random(0, 5)
Endif

SceneEnd()
timeout(24, big_ego)