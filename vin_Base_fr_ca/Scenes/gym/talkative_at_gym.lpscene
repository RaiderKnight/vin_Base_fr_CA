WHAT: do_cardio, lift_weights
WHERE: sports_centre
WHEN: 0 - 24
WHO: none
OTHER: Random(0, 100) < 5

SceneStart()
Actor = generatePerson()
Actor.dress()
Actor.show(2)

"Je continuais mon entraînement quand <Actor.un_or_une> <Actor.gars_or_fille> très <Actor.bavard_or_bavarde> s'approche de moi pour discuter encore et encore ... Ça devient assez étourdissant maintenant."
0:: "L'ignorer"
1:: "Lui fermer la gueule"

If 0
    "J'ai fait semblant de l'écouter et j'ai attendu jusqu'à ce qu'<Actor.il_or_elle> termine son 'inter' 'minable sujet' de conversation."
    masochist += Random(0, 0.5)
Else
    Player(Angry):: "S'cuse <Actor.le_or_la> <Actor.gland_or_glande>, euh <Actor.le_or_la> <Actor.grand_or_grande>, je suis ici pour m'entraîner, pas pour faire du social."
    masochist -= Random(0, 0.5)
    Actor:rapportwithplayer -= Random(0, 5)
Endif

SceneEnd()
timeout(24, talkative_at_gym)