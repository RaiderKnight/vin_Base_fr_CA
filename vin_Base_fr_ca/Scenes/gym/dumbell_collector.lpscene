WHAT: do_cardio, lift_weights
WHERE: sports_centre
WHEN: 0 - 24
WHO: none
OTHER: Random(0, 100) < 5

SceneStart()
Actor = generatePerson()
Actor.dress()
Actor.show(2)

"Je n'ai pas pu m'empêcher de remarquer <Actor.un_or_une> <Actor.gars_or_fille> faire des supersets et s'approprie la moitié des haltères du gymnase autour de <Actor.lui_or_elle>, y compris ceux dont j'ai besoin en ce moment."
0:: "L'ignorer"
1:: "Lui lancer un commentaire"

If 0
    "Bof, je m'en fou. Je vais juste faire autre chose en attendant qu'<Actor.il_or_elle> ait fini."
    masochist += Random(0, 0.5)
Else
    Player(Angry):: "Hey <Actor.le_or_ma> <Actor.gros_or_grande>! Tu sais que t'es pas <Actor.le_or_la> <Actor.seul_or_seule> à vouloir les utiliser!"
    masochist -= Random(0, 0.5)
    Actor:rapportwithplayer -= Random(0, 5)
Endif

SceneEnd()
timeout(24, dumbell_collector)