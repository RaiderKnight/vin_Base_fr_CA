WHAT: work
WHERE: work
WHEN: 0 - 24
WHO: Boss = getSpecific(Boss)
OTHER: jobexperience > 70

    SceneStart()
    Warning = false
    Actor = getPerson(tag_secretary)
    If !Actor.isValid()   // HIRE
        "Mon boss veut me rencontrer dans son bureau"
        Boss.dress()
        Boss.show()
        Boss:: "Vous êtes un employé avec de l'ancienneté maintenant. Vous avez droit à votre propre bureau et à une secrétaire pour vous aider. Pourquoi ne pas interviewer quelques candidats et embaucher celui que vous aimez?"
        Player::"Merci beaucoup boss, d'accord je vais passer quelques entrevues..."
        Boss.hide()
        "Quelques jours plus tard ... ..."
        Actor = generatePersonTemporary()
        Actor.dress()
        Actor.show()
        Actor:: "Merci de votre considération et d'avoir pris le temps de me passer en entrevue. J'attend votre appel!"
        "<Actor.name> semble être le meilleur candidat parmi ceux a qui j'ai passé en entrevue."
        If Actor.isInterestedIn(Player) && Actor:perversion + Actor:attractiontoplayer > 100
            "Je n'ai pas pu m'empêcher de remarquer que <Actor.name> me cruisait un peu pendant l'interview. <Actor.Il_or_Elle> sera certainement <Actor.un_or_une> secrétaire <Actor.licheux_or_licheuse>, dans le bon sens cette fois-ci. Mais <Actor.ils_or_elles> sont souvent moins travaillant, mais ça compense..."
        Endif
        "Que dois-je faire de ce candidat au poste de secrétaire?"
        0:: "L'Embaucher "
        1:: "Rechercher un candidat plus tard"
        2:: "Je n'ai pas besoin de secrétaire en fait"
        
        If 0
            Actor.makePermanent()
            Actor.addColleague()
            Actor.setActorVar(tag_secretary, 1)
            exchangeContact(Actor)
            "J'ai décidé d'engager <Actor.name> comme secrétaire."
        Elseif 1
            "Cherchons un autre candidat un autre jour."
        Else
            "Arrêtons complètement de chercher une secrétaire. Je n'ai pas l'habitude d'en avoir une. Je ne sais pas quoi lui donner comme job à faire..."
            timeout(2000, secretary_cms)
        Endif
    Else
        Actor.dress()
        Actor.show()
        "<Actor.name> est maintenant <Actor.mon_or_ma> secrétaire."
        0:: "<Actor.Il_or_Elle> fait un excellent tavail"
        1:: "Je <Actor.le_or_la> congédie"
        
        If 0       
            stage = Actor.getActorVar(tag_secretary)
            If stage < 2
                "Que dois-je demander à <Actor.name> de faire aujourd'hui?"
                0:: "M'aider à avancer certains dossiers"
                1:: "Suggérer une relation sexuelle"
                
                If 0
                    jobperformance += Actor:intelligence*0.01
                    Actor:attractiontoplayer += 1
                    "Dois-je flirter un peu <Actor.name>?"
                    0:: "Oui"
                    1:: "Non"
                    
                    If 0
                        Actor:attractiontoplayer += 2
                        "J'ai profité du moment que nous travaillions ensemble pour flirter <Actor.name>."
                    Endif
                Else
                    If Actor:perversion + Actor:attractiontoplayer > 100
                        Actor(Flirty):: "Cooool, for sure ... Je suppose que c'est ainsi que les choses fonctionnent avec vous. Je dois m'intégrer à la culture d'entreprise."
                        "A partir de maintenant, j'ai décidé que <Actor.name> ira au-delà de sa fonction de secrétaire et me servira également de d'autres manières."
                        Player(Flirty):: "Oui, je crois que nous allons faire un bon Team nous deux..."
                        Actor.setActorVar(tag_secretary, 2)
                    Else
                        Actor:: "C'est une blague, j'espère?"
                        Player(Flirty):: "Oui désolé, je ne voulais pas t'offenser"
                        "Eh bien, au moins <Actor.il_or_elle> n'a pas fait de plainte aux RH."
                    Endif
                Endif
            Else  // RELATIONSHIP STARTED
                "Que dois-je demander à <Actor.name> de faire aujourd'hui?"
                0:: "M'aider à avancer certains dossiers"
                1:: "Suggérer une petite vite"
                
                If 0
                    jobperformance += Actor:intelligence*0.01
                Else
                    "Où allons-nous baiser?"
                    0:: "Dans mon bureau"
                    1:: "Ici même"
                    
                    If 0
                        Player(Flirty):: "<Actor.name>, cela vous dérangerait-il de venir faire un tour dans mon bureau j'aimerais discuter avec vous?"
                        Actor(Flirty):: "Bien sûr, Bien sûr, boss."
                        "A en juger par l'expression du visage <Actor.name>, <Actor.il_or_elle> sait déjà exactement pourquoi je qu'<Actor.il_or_elle> me rencontre dans mon bureau."
                        setBackground(community_centre)
                        Player(Flirty):: "Ferme la porte et barre-la..."
                        Actor(Flirty):: "Que puis-je faire pour vous, sexy boss?"
                        0:: "Sexe oral"
                        1:: "Sexe complet"
                        
                        If 0
                            Filter(Oral)
                            Sex(Player, Actor)
                            Random
                                Actor2 = getSpecific(Colleague)
                                Actor2 = getSpecific(Dating)
                                Actor2 = getRelative()
                            endRandom
                            
                            If Actor2.isValid()
                                Actor.hide()
                                Actor2.dress()
                                Actor2.show()
                                "Pendant que <Actor.name> et moi étions en train de baiser à l'arrière de mon bureau, <Actor2.name> est entré dans mon bureau avec quelque chose à dire. <Actor.name> s'est rapidement caché sous le bureau tout en continuant à me licher <Player.ma_or_mon> <Player.queue_or_clito>. "
                                "<Actor2.name> me demande des informations à propos d'un dossier. Pendant qu'<Actor2.il_or_elle> pose ces questions, <Actor.name> continue sa job en-dessous du bureau sans que <Actor2.name> s'en rendre compte."
                                "Après notre conversation, <Actor2.name> sort du bureau et referme la porte derrière-<Actor2.lui_or_elle>."
                                Actor2.hide()
                                Actor.show()
                                Actor:: "N'était-ce pas excitant? As-tu trouvé ça cool que je n'arrête pas, entouka moi oui?"
                                Actor:: "Maintenant qu'<Actor2.il_or_elle> est partqu'<Actor2.i_or_ie>, je veux que tu vienne dans ma <Player.bouche_or_face> <Actor.mon_or_ma> chérie."
                                Filter(Oral)
                                Sex(Player, Actor)                                                              
                            Endif
                        Else
                            Sex(Player, Actor)
                            Random
                                Actor2 = getSpecific(Colleague)
                                Actor2 = Boss
                            endRandom
                            
                            If Actor2.isValid() && Random(0, 100) > sneak
                                Actor2.dress()
                                Actor2.show()
                                "Pendant que <Actor.name> et moi étions en train de baiser, <Actor2.name> entre dans mon bureau et nous surprend en flagrant délit."
                                If Actor2:masochist - Actor:perversion > 0
                                    Actor2(Embarrassed):: "Oh my god, j'ai absolument rien vue ... what the fuck!"
                                    "<Actor2.name> s'est enfui chez <Actor2.lui_or_elle> pour la journée, clairement embarrassé."
                                Else
                                    Actor2:: "Euh!, Désolé, j'ai pas le choix de vous le dire mais est-ce que vous savez tous les deux que vous n'êtes pas très discrets, je peux vous entendre de mon bureau?"
                                    "Comment dois-je réagir à la situation?"
                                    0:: "Inviter <Actor2.name> à notre pause sexe"
                                    1:: "Je te rappel <Actor2.name> que je suis le boss, sort et ferme la porte"
                                    
                                    If 0 && Actor2:perversion > 50
                                        Actor2:: "Sérieux, cool, je me déshabille tout de suite..., j'vais barrer la porte..."
                                        Player:: "Non, laisse-la débarrée,... plus il y a de monde, plus on a du fun!"
                                        Sex(Player, Actor, Actor2)
                                    Else
                                        Warning = true
                                    Endif
                                Endif
                            Endif                            
                        Endif
                    Else
                        "J'ai trop envie et trop excité pour aller baiser dans mon bureau, baisons simplement ici dans le bureau vitré de <Actor.name>."
                        Sex(Player, Actor)
                        Random
                            Actor2 = getSpecific(Colleague)
                            Actor2 = Boss
                        endRandom
                        
                        If Actor2.isValid()
                            Actor2.dress()
                            Actor2.show()
                            "Pendant que <Actor.name> et moi étions en train de baiser, <Actor2.name> entre dans mon bureau et nous surprend en flagrant délit."
                            If Actor2:masochist - Actor:perversion > 0
                                Actor2(Embarrassed):: "Oh my god, j'ai absolument rien vue ... what the fuck!"
                                "<Actor2.name> s'est enfui chez <Actor2.lui_or_elle> pour la journée, clairement embarrassé."
                                "Prude, PD ou vraiment pas fait pour mon entreprise..."
                            Else
                                Actor2:: "Woah! Vous êtes vraiment pas gêné de baiser devant tout le monde dans le bureau."
                                "Comment dois-je réagir à la situation?"
                                0:: "Inviter <Actor2.name> à notre pause sexe"
                                1:: "Je te rappel <Actor2.name> que je suis le boss, sort et ferme la porte"
                                
                                If 0 && Actor2:perversion > 50
                                    Actor2:: "Ben là, cool, je me déshabille tout de suite ..."
                                    Sex(Player, Actor, Actor2)
                                Else
                                    Warning = true
                                Endif
                            Endif
                        Endif           
                    Endif
                Endif
            Endif
        Else
            "J'ai décidé de licencier <Actor.name> du poste de secrétaire."
            Actor.removeColleague()
            Actor.setActorVar(tag_secretary, 0)
        Endif
    Endif
    
    If Warning
        "Pour mon comportement indécent récent avec ma secrétaire, j'ai reçu un avertissement venant des RH. Cela aura sûrement un impact sur ma prochaine évaluation."
        jobperformance -= 10
    Endif
    SceneEnd()
    timeout(100, secretary_cms)