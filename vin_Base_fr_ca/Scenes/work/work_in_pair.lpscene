WHAT: work
WHERE: work
WHEN: 8 - 18
WHO: Colleague = getSpecific(Colleague)
OTHER: Random(0, 100) < 0.02*intelligence

SceneStart()
setBackground(work)
Colleague.dress()
Colleague.show(2)

"C'est une pratique commerciale courante de travailler en duo sur un petit projet. Récemment, j'étais dans un projet avec <Colleague.name>. Ce fut un succès. Cependant, devant les plus hauts gradés, quel crédit devrais-je prendre ?"
0:: masochist > -50:: "Donner tous les mérites à <Colleague.name>"
1:: masochist < 50:: "Donnez-moi tous les mérites"
2:: "Répartir les mérites également"

If 0
    Colleague:rapportwithplayer += Random(0, 5)
    "Je donne à <Colleague.name> tous les mérites. Notre relation semblait s'être améliorée en conséquence."
    masochist += Random(0, 1)
Elseif 1
    Colleague:rapportwithplayer -= Random(0, 5)
    jobperformance += Random(0, 3)
    "Je me donne tous les mérites, ce qui a sans aucun doute choqué <Colleague.name> qui je m'en fou."
    masochist -= Random(0, 1)
Else
    Colleague:rapportwithplayer += Random(0, 2)
    jobperformance += Random(0, 1)
    "J'ai donné des crédits là où ils sont dus et nous avons tous les deux été félicités par nos patrons."
Endif

SceneEnd()
timeout(96, work_in_pair)