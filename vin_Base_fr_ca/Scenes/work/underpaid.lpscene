WHAT: work
WHERE: work
WHEN: 8 - 20
WHO: none
OTHER: Random(0, 100) < 0.1*jobperformance

    SceneStart()
    "Les salaires sont censés être confidentiels, mais souvent ils ne le sont pas. J'ai récemment découvert qu'un de mes collègues, qui a le même titre et la même expérience que moi, est payé un peu plus que moi."
    0:: "Exigez un salaire équitable"
    1:: "Peut-être pas"

    If 0
        "J'ai décidé de parler à mon patron et de lui faire part de mon inquiétude d'être injustement sous-payé."
        If Random(0, 500) < interpersonal + jobperformance
            "J'ai réussi à convaincre mon patron d'égaler mon salaire avec celui de mon collègue."
            Salary = getSalary()
            Salary *= Random(1.1, 1.3)
            Salary.setSalary()
            timeout(5000, underpaid)
        Else
            Actor = getSpecific(Boss)
            Actor.dress()
            Actor.show()
            "La discussion ne s'est pas du tout bien déroulée ... Non seulement mon patron a rejeté ma demande, mais il était visiblement en tabarnak"
            0:: "Offrir du sexe en échange d'une augmentation"
            1:: "Abandonner"
            
            If 0
                If Actor.hadSex() || Actor:attractiontoplayer > 40 || [Actor:perversion > 70 && Actor.isInterestedIn(Player)]
                    Actor:: "Très bien ... puisque vous êtes si persistant  ..."
                    Sex(Actor, Player)
                    "J'ai réussi à 'convaincre' mon patron d'égaler mon salaire avec celui de mon collègue."
                    Salary = getSalary()
                    Salary *= Random(1.1, 1.3)
                    Salary.setSalary()
                    timeout(5000, underpaid)                    
                Else
                    Actor:: "Comment osez-vous suggérer quelque chose du genre? Sortez de mon bureau!"
                    jobperformance -= Random(0, 10)
                Endif
            Else
                jobperformance -= Random(0, 5)            
            Endif
        Endif
    Else
        "Ce n'est pas une bonne idée. Les entreprises ne cèdent pas aussi facilement aux demandes du personnel pour des salaires plus élevés. Tout ce que je ferai, c'est de contrarier mon patron."
    Endif

    SceneEnd()
    timeout(500, underpaid)