WHAT: work
WHERE: work
WHEN: 0 - 24
WHO: Actor = getSpecific(Boss); If Actor.hadSex() && Random(0, 100) > Actor:intelligence
OTHER: none

    SceneStart()
    Actor.dress()
    Actor.show()
    "<Actor.Mon_or_Ma> boss a été très bête aujourd'hui, pourtant la société voit beaucoup de travail à l'horizon, ce qui est bon pour les revenus, mais il y a quelque chose qui <Actor.le_or_la> stress!"
    0:: "Offrir de soulager son stress avec du sexe"
    1:: "Peut-être pas"
    
    If 0
        Sex(Actor, Player)
        jobperformance += 5
        "Grâce à mes efforts, l'humeur de <Actor.mon_or_ma> boss s'est améliorée et l'entreprise produit des résultats encore meilleurs."
    Else
        "Je vais simplement laisser <Actor.mon_or_ma> boss s'occuper de ces problèmes. <Actor.Il_or_Elle> est mieux rémunéré pour gérer son stress."
    Endif
    SceneEnd()
    timeout(200, relieve_boss)