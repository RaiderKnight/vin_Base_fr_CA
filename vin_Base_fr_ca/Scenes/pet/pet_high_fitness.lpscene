WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getCreature(); If Random(70, 100) < Actor:fitness 
OTHER: isAtHome()

    SceneStart()
    Actor.show(2)
    "<Actor.name> semble si en forme et énergique ces temps-ci. Ces visites chez le vétérinaire sont vraiment payantes."
    "Je peux dire que <Actor.he_or_she> apprécie mes bons soins."
    Actor:rapportwithplayer += Random(0, 5)
    Actor:attractiontoplayer += Random(0, 5)
    SceneEnd()
    timeout(200, pet_high_fitness, pet_low_fitness)