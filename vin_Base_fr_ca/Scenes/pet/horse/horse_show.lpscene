WHAT: all, -sleep, -nap
WHERE: all
WHEN: 6 - 12
WHO: Actor = getCreature(); If Actor.isCreatureType(Horse)
OTHER: none

    SceneStart()
    setBackgroundCustom(stable)
    Actor.show(2)
    "Il y a un concours d'équitation aujourd'hui. Dois-je emmener <Actor.name> et participer?"
    0:: "Oui"
    1:: "Non"
    
    If 0
        "Nous sommes allés nous amuser au concours d'équitation. Nous n'étions pas trop professionnels, nous n'avons donc rien gagné, mais au moins c'était un lien de qualité entre le propriétaire et le cheval, malgré quelques blessures mineures subies de notre côté."
        "J'ai aussi rencontré et échangé des contacts avec quelques amis amoureux des chevaux."
        Actor:fitness -= Random(0, 5)
        energy -= Random(25, 75)
        Actor:attractiontoplayer += Random(0, 10)
        Actor:rapportwithplayer += Random(0, 10)
        count = 0
        While count < 5
            count += 1
            Actor2 = generatePerson()
            exchangeContact(Actor2)
        Endwhile
    Else
        "Ni moi ni mon cheval ne sont en état de faire un concours d'équitation en ce moment."
    Endif
    
    SceneEnd()
    timeout(500, horse_show)