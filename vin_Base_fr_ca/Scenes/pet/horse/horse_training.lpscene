WHAT: all, -sleep, -nap
WHERE: all, -work
WHEN: 6 - 18
WHO: Actor = getCreature(); If Actor.isCreatureType(Horse)
OTHER: none

    SceneStart()
    setBackgroundCustom(stable)
    Actor.show(2)
    "J'entraîne mon cheval aujourd'hui. Malheureusement, <Actor.name> semble plus têtu que d'habitude et n'écoute aucune de mes instructions"
    0:: "Soyez plus assertif"
    1:: "Montrez plus de patience"
    
    If 0
        "Je commence à élever davantage la voix pour m'assurer que mon cheval sait qui est en charge ici."
        masochist -= Random(0, 2)
        Actor:attractiontoplayer -= Random(0, 2)
        Actor:rapportwithplayer -= Random(0, 2)
    Else
        "Ce n'est qu'un de ces mauvais jours. Faisons juste preuve d'un peu plus de patience et soyons gentils et <Actor.il_or_elle> devrait éventuellement commencer à écouter mes instructions."
        masochist += Random(0, 2)
        Actor:attractiontoplayer += Random(0, 2)
        Actor:rapportwithplayer += Random(0, 2)
    Endif
    
    SceneEnd()
    timeout(200, horse_training)