WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getCreature(); If Random(0, 40) > Actor:fitness 
OTHER: isAtHome()

    SceneStart()
    Actor.show(2)
    "<Actor.name> semble si malade et léthargique c'est temps-ci. J'ai vraiment besoin d'emmener <Actor.name> chez le vétérinaire plus souvent"
    "Même que <Actor.name> commence à se demander si je suis vraiment son propriétaire, tellement je <Actor.le_or_la> néglige."
    Actor:rapportwithplayer -= Random(0, 5)
    Actor:attractiontoplayer -= Random(0, 5)
    SceneEnd()
    timeout(200, pet_high_fitness, pet_low_fitness)