WHAT: all
WHERE: all, -home
WHEN: 0-24
WHO: Actor = getCompanion(); If Random(50, 200) < Actor:attractiveness && !Actor.isProstitute()
OTHER: none

    SceneStart()
    Actor2 = generatePersonTemporary()
    Actor2.dress()
    Actor2.show(2)
    Actor2(Flirty):: "Humm! Tu es un régal pour mes yeux? Quel est ton nom? Je suis <Actor2.name>. Tu aimerais sortir quelque part..."
    "Check <Actor2.le_or_la> <Actor2.cave_or_bitch> qui essait de me voler <Player.mon_or_ma> <Player.chum_or_blonde>! Cruise <Player.mon_or_ma> <Player.chum_or_blonde> comme ça tout en m'ignorant complètement. Comme si je n'étais même pas là!"
    0:: "Arrêter son show"
    1:: "Ignorer"
    
    If 0
        Player(Angry):: "Hey <Actor2.le_or_la> zouf! Tu ne vois pas que nous sommes en couple!"
        Actor:attractiontoplayer += Random(0, 2)
        masochist -= Random(0, 1)
    Else
        "Inutile de capoter ... <Actor.name> est le type de <Actor.gars_or_fille> est trop open côté sexe pour que je sois intéressé."
        Actor:attractiontoplayer -= Random(0, 2)
        masochist += Random(0, 1)
    Endif
    SceneEnd()
    timeout(24, date_hit_on)