WHAT: all
WHERE: work
WHEN: 12 - 20
WHO: Ex = getSpecific(ExDating); If !Ex.hasRelationship(Siblings)
OTHER: Random(0, 200) < jobexperience

    SceneStart()
    "J'ai été envoyé à une conférence industrielle aujourd'hui. La journée s'est déroulée sans incident jusqu'à ..."
    Actor = Ex.generatePersonMatchRace()
    Actor:age => Ex:age + Random(-10, 10)
    Actor:age += 1
    Actor.randomizeFace()
    Actor.randomizeHairs()
    Actor.matchLastName(Ex)
    addNpcRelationship(Siblings, Actor, Ex)
    Actor.dress()
    Actor.show(2)
    Actor(Happy):: "Bonjour, je suis <Actor.name>. Ravi de vous rencontrer."
    "Nous avons continué la conversation pendant un petit moment. La conférence se termine maintenant ..."
    0:: "Échanger nos cartes d'affaire"
    1:: "Peut-être pas"
    
    If 0
        exchangeContact(Actor)
        Player(Surprised):: "Votre nom de famille est <Actor.name_last> ... Vous ne connaissez pas <Ex.name> <Ex.name_last> par hasard?"
        Actor(Surprised):: "Bien sûr que je connais <Ex.name>. <Ex.Il_or_Elle> est <Ex.mon_or_ma> <Ex.frere_or_soeur>. Vous connaissez <Ex.mon_or_ma> <Ex.frere_or_soeur> alors?"
        0:: "Ouais  ..."
        1:: "<Ex.name> est mon ex"
        
        If 0
            Player(Embarassed):: "Ouais, juste une connaissance. Je ne <Ex.le_or_la> connait pas si bien pour être honnête."
        Else
            Player(Sad):: "Avant, nous étions en couple ... <Ex.name> ne t'a jamais parler de moi, j'imagine ta famille alors ..."
            "Je ne sais même pas pourquoi j'ai été si honnête à ce sujet. Mon aveu a rendu la conversation très difficile après ..."
            Actor:attractiontoplayer -= Random(0, 40)
        Endif
    Endif

    SceneEnd()
    timeout(1000, ex_boss, ex_sibling)