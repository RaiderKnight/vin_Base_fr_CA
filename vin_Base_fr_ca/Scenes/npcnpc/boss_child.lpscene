WHAT: all
WHERE: work
WHEN: 8 - 19
WHO: Actor = getSpecific(Boss); If Actor:age > 40 && !Actor.hasRelationship(ParentChild)
OTHER: none

    SceneStart()
    Actor2 = Actor.generatePersonMatchRace()
    Actor2.matchLastName(Actor)
    Actor2:age => Actor:age - Random(20, 40)
    Actor2:age += 1
    Actor2.randomizeHairs()
    Actor2.randomizeFace()
    Actor2.dress()
    addNpcRelationship(ParentChild, Actor, Actor2)
    Actor2.show(2)
    "Aujourd'hui, <Actor2.le_or_la> <Actor2.fils_or_fille> de mon patron a rejoint l'entreprise pour un stage de courte durée. Le népotisme à son meilleur!"
    0:: "Me rapprocher de <Actor2.name>"
    1:: "On s'en câliss?"
    
    If 0
        If !Actor2.isInterestedIn(Player) || Actor2:attractiontoplayer > 20
            "Au cours du stage de <Actor2.name>, j'ai réussi à me rapprocher de <Actor2.lui_or_elle>. Nous avons même échangé nos numéros pour se rencontrer hors du travail. Que de bons mots pour moi devant le patron."
            exchangeContact(Actor2)
            jobperformance += Random(0, 5)
        Else
            "Malheureusement, je n'ai pas réussi à me rapprocher de l'arrogan<Actor2.t_or_te> <Actor2.petit_or_petite> <Actor2.prince_or_princesse>"
            "J'avoue, qu'<Actor2.il_or_elle> n'a surement pas le temps de passer du temps avec <Actor2.un_or_une> pauvre comme moi. <Actor2.Il_or_Elle> ne m'a au moins pas rapporté au boss ..."
            jobperformance -= Random(0, 10)
        Endif
    Endif
    
    SceneEnd()
    timeout(1000, boss_child)