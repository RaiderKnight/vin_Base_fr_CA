WHAT: all
WHERE: university:work
WHEN: 8 - 18
WHO: Actor = getSpecific(Lecturer); If Actor:rapportwithplayer > 10
OTHER: none

    SceneStart()
    dressFormal()
    Actor.dress()
    Actor.show(2)
    Actor(Happy):: "<Player.name>, tu pourrais peut-être me remplacer pour la classe <Actor.Major> plus tard dans la journée? Je dois courir. J'ai quelque chose d'urgent à faire."
    0:: "Oui"
    1:: "Non"
    
    If 0
        Player(Happy):: "Bien sûr, je serai ravi de t'aider."
        Actor:rapportwithplayer += Random(0, 2.5)
        karma += 2.5
    Else
        Player(Sad):: "Désolé, j'ai un autre cours à ce moment-là. Vous devriez peut-être demander à un autre prof? "
        Actor:rapportwithplayer -= Random(0, 2)
    Endif
    SceneEnd()
    timeout(350, substitute_lecturer)