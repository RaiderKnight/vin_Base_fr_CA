WHAT: conduct_research
WHERE: university:work
WHEN: 0-24
WHO: Actor = getSpecific(Lecturer); Actor2 = getSpecific(Lecturer); If Actor:rapportwithplayer > 0 && Actor2:rapportwithplayer > 0
OTHER: Random(0, 100) < intelligence

    SceneStart()
    dressFormal()
    Actor.dress()
    Actor.show(2)
    Actor2.dress()
    Actor2.show(3)
    "Deux de mes collègues, <Actor.name> et <Actor2.name>, me demandent tous les deux de travailler avec eux sur un projet de recherche ... Je n'ai cependant pas le temps de m'engager dans les deux."
    "Qui choisir?"
    0:: "Travailler avec <Actor.name>"
    1:: "Travailler avec <Actor2.name>"
    
    If 0
        Actor:rapportwithplayer += Random(0, 2)
        Actor2:rapportwithplayer -= Random(0, 2)
    Else
        Actor2:rapportwithplayer += Random(0, 2)
        Actor:rapportwithplayer -= Random(0, 2)    
    Endif
    SceneEnd()
    timeout(200, compete_research_partner)