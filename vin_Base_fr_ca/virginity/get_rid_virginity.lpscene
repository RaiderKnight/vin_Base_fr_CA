WHAT: all
WHERE: home
WHEN: 0 - 24
WHO: none
OTHER: Player.isVirgin() && !isWithCompanion()


    SceneStart()
    "Je suis vierge en ce moment..."
    0:: "Et j'en suis fier!"
    1:: "Et je veux me faire dévierger!"
    
    If 0
        "Et c'est complètement par choix. Je me réserve pour quelqu'un de spécial."
    Else
        "Pour être honnête, j'ai hâte de me faire dévierger et de vivre ma vie avec autant de confiance que le reste de mes pairs. Mais qui choisir pour ma première baise? "
        0:: "Quelqu'un que je connais?"
        1:: "N'importe qui?"
        2:: "Peut-être plus tard, finalement!"
        
        If 0
            "Bien sûr, je serais surement plus à l'aise d'avoir ma première relation sexuelle avec un(e) ami en qui je peux avoir confiance. "
            selectNPC()
            Actor = getSelectedNPC()
            
            If Actor:rapportwithplayer > 20 && Actor:attractiontoplayer > 5 && Actor:perversion > 30 && !Actor.isVirgin()
                Actor.dress()
                Actor.show()
                "Inutile de dire que, de péter la cerise de quelqu'un ne se présente pas à tous les jours, c'est pourquoi <Actor.name> a sauté sur l'occasion. "
                Sex(Player, Actor)
            Else
                "Malheureusement, <Actor.name> a refusé mes avances ..."
            Endif
        Elseif 1
            "N'importe qui, est le choix le plus intelligent - pas de conditions - pas de problèmes futurs."
            "J'ai mis une annonce sur Craiglist offrant ma virginité gratuitement et j'ai l'impression d'avoir bogué le site. Jamais eu autant de réponses! "
            Actor = generatePersonTemporary()
            While !Player.isInterestedIn(Actor)
                Actor = generatePersonTemporary()
            Endwhile
            SecondScreen(Actor)
            Actor.dress()
            Actor.show()
            "La plupart sont assez bizarres, mais <Actor.name> semble être pas si pire. Beau body et <Actor.il_or_elle> a l'air propre. "
            0:: "Baiser avec <Actor.name>"
            1:: "Peut-être une autre fois, finalement!"
            
            If 0
                SecondScreen()
                Sex(Player, Actor)
            Endif
        Endif    
    Endif
    SceneEnd()
    timeout(200, get_rid_virginity)