WHAT: all
WHERE: all
WHEN: 21 - 5
WHO: Actor = getPerson(true); If Actor.isDeflowerer() && Actor:perversion > 70 && Actor:masochist < -30 && Actor:attractiontoplayer > 10 && !Actor.isDating()
OTHER: !isWithCompanion()

    SceneStart()
    SecondScreen(Actor)
    Actor.dress()
    Actor.show()
    Actor:: "Je suis excitée. Viens, je veux te baiser!"
    Actor:: "Pourquoi tu me fais attendre si longtemps? Ne fait pas comme si tu voulais pas."
    Actor:: "Je sais que tu fantasme sur moi tous les jours. Pis moi j'ai juste hâte de te voir nue et déguster ton corps que j'adore."
    "De quel genre de déclaration s'agit-il? <Actor.name> est comme on dit très direct, mais comme <Actor.name> me donne l'impression de vouloir que du sexe, suis-je vraiment d'accord pour baiser avec <Actor.name> et d'être son 'fuck friend'."
    0:: "Baiser avec <Actor.name>"
    1:: "Gardez une certaine dignité."
    
    If 0
        masochist += 1
        Dest = Actor.getBuilding(home)
        moveTo(Dest)
        Sex(Actor, Player)
    Else
        "Je devrais juste admettre que j'ai fait une erreur de m'avoir fait dévierger par <Actor.name>. Je devrais enlever cette relation de ma tête et ne plus me laisser exploiter. "
        masochist -= 1
    Endif
    
    SceneEnd()
    timeout(100, deflowerer_takes_advantage)