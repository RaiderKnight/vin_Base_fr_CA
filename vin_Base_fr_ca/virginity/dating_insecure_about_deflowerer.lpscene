WHAT: all
WHERE: all
WHEN: 0 - 24
WHO: Actor = getPerson(true); Actor2 = getSpecific(Dating); If Actor.isDeflowerer() && !Actor.isDating() && Actor2:masochist < 0
OTHER: !OpenRelationship()

    SceneStart()
    SecondScreenIfHidden(Actor)
    Actor.dress()
    Actor.show()
    Actor2.dress()
    Actor2.show()
    "Mon amour, n'aime pas le fait qu'<Actor2.il_or_elle> ne soit pas la première personne avec qui j'ai couché ..."
    "Pire encore, <Actor2.name> a récemment découvert que je restais toujours en contact avec ma première relation amoureuse, <Actor.name>, et que je n'avais pas l'intention d'arrêter de lui parler."
    "Finallement aujourd'hui, <Actor2.name> a exigé que je bloque <Actor.name> de mon téléphone et de mes réseaux sociaux."
    "Que dois-je répondre?"
    0:: "Pas question!"
    1:: "D'accord ..."
    
    If 0
        "C'est ridicule! Ce qui s'est passé avant même que nous nous soyons rencontrés ne te concerne pas <Actor2.name>. <Actor.name> est une personne spéciale dans ma vie quoi qu'il arrive."
        Actor2:attractiontoplayer -= 10
    Else
        "Notre relation en souffre vraiment. Même si <Actor.name> a été mon premier et j'aime bien lui parlé, <Actor.name> n'est pas une personne aussi importante dans ma vie que mon <Actor2.name>."
        "Alors pas le choix, je bloque <Actor.name> de mes contacts!"
        blockContact(Actor)
        Actor2:attractiontoplayer += 5
    Endif
    SceneEnd()
    timeout(500, dating_insecure_about_deflowerer)