WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    Player.hide()
    "Ensuite, à quoi ressemble votre personnage en tant que personne? L'écran suivant vous permettra de changer ses attributs."
    SceneEnd()
    OpenMenu(Stat)
    followUp(tf_afterstat)